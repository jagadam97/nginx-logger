version: "3.8"
services:
  nginx-logger:
    image: ghcr.io/jagadam97/nginx-logger:latest
    environment:
      BATCH_SIZE: "${BATCH_SIZE}"
      BATCH_DELAY: "${BATCH_DELAY}"
      LOG_FILE_PATH: "${LOG_FILE_PATH}"
      INFLUX_URL: "${INFLUX_URL}"
      INFLUX_TOKEN: "${INFLUX_TOKEN}"
      INFLUX_ORG: "${INFLUX_ORG}"
      INFLUX_BUCKET: "${INFLUX_BUCKET}"
      # nginx stub_status endpoint. Unset = feature off.
      STUB_STATUS_URL: "${STUB_STATUS_URL}"
      STUB_INTERVAL: "${STUB_INTERVAL:-10s}"
      # Published port and the port the app binds to are kept in lockstep.
      API_PORT: "${API_PORT:-8124}"
    ports:
      - "${API_PORT:-8124}:${API_PORT:-8124}"
    volumes:
      # NPM's /data/logs on the host. Read-only: the collector only tails.
      - "${NPM_LOG_DIR:-/npm/data/logs}:/var/log:ro"
    # Join NPM's network so stub_status is reachable by container name.
    networks:
      - wg_default

networks:
  wg_default:
    external: true
